/*
 * Netlify redirects configuration. This file ensures that when a user navigates
 * to the /beta-signup path on the deployed site, Netlify will serve the
 * replyflow_signup_page.html file without rewriting the URL (HTTP 200).
 *
 * See Netlify docs for more details: https://docs.netlify.com/routing/redirects/
 */

# Redirect the beta-signup route to the signup page
/beta-signup  /replyflow_signup_page.html  200